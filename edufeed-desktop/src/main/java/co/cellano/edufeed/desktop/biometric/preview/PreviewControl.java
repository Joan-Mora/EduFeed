package co.cellano.edufeed.desktop.biometric.preview;

/** Contrato simple para vistas con vista previa en vivo (iniciar/detener). */
public interface PreviewControl {
    void start();
    void stop();
}
